import("std/list") as List
import("std/result") as Result
import("std/flow") as Flow

let a = List.sort_int([2,1])
let b = List.dedupe_sorted_int([1,1,2])
let c = Result.andThen(Result.ok(1), fn(x){ Result.ok(x+1) })
let d = Flow.pipe(1, [fn(x){x+1}, fn(x){x+1}])

{ a: a, b: b, c: c, d: d }
