import("std/grow") as Grow

fn step(s) {
  if s.i < 10 then { seed: { i: s.i + 1 }, value: s.i } else null
}

let xs = Grow.unfold({ i: 0 }, 100, step) in
let _ = emit({stage: "unfold", len: 10}) in
xs
